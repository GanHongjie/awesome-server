spring:
  profiles:
    active: dev
    include: mybatis-plus-config,log
  devtools:
    add-properties: false
---
#开发环境配置

server:
  port: 8081

---
#测试环境配置
spring:
  profiles: stg

server:
  port: 8082

---
#生产环境配置
spring:
  profiles: prd

server:
  port: 8083
